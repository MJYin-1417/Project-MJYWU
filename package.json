{
    "scripts":{
        "build": "cd listing && npm run build",
        "start": "cd listifyServer && node app.js",
        "install-client": "cd listing && npm install",
        "heroku-postbuild": "npm run install-client && npm run build"
    },
    "dependencies": {
        "concurrently": "^7.2.2",
        "dotenv": "^16.0.1",
        "express": "^4.18.1",
        "nodemon": "^2.0.18"
    },
    "engines" : { 
        "npm" : "8.11.0",
        "node" : "16.15.1"
      }
}
